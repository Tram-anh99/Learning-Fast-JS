# 🌾 Agriculture Management - FastAPI Backend

> API server kết nối PostgreSQL với FastAPI framework

## 🚀 Quick Start

### 1. Cài Đặt Dependencies

```bash
# Tạo virtual environment (khuyến nghị)
python3 -m venv .venv
source .venv/bin/activate  # macOS/Linux
# .venv\Scripts\activate   # Windows

# Cài packages
pip install -r requirements-minimal.txt
```

### 2. Cấu Hình Database

Sửa file `.env`:

```bash
DB_HOST=localhost
DB_PORT=5433          # Port PostgreSQL của bạn
DB_NAME=postgres      # Database name
DB_USER=postgres      # Username
DB_PASSWORD=123456    # Password (hoặc 000000)
DB_SCHEMA=nongsan     # Schema name
```

### 3. Test Kết Nối

```bash
./test_db.sh
```

### 4. Khởi Động Server

```bash
./start.sh

# Hoặc thủ công:
python app.py

# Hoặc dùng uvicorn:
uvicorn app:app --reload
```

Server chạy tại: **http://localhost:8000**

---

## 📚 API Documentation

### Swagger UI (Interactive)
👉 **http://localhost:8000/docs**

### ReDoc
👉 **http://localhost:8000/redoc**

---

## 🗂️ Cấu Trúc Project

```
Backend/
├── app.py                    # Main FastAPI application
├── config.py                 # Configuration & settings
├── database.py               # Database connection & session
├── schemas.py                # Pydantic models (request/response)
│
├── models/                   # SQLAlchemy ORM models
│   ├── __init__.py
│   ├── vung_trong.py         # Farm zones
│   ├── loai_cay.py           # Crop types
│   ├── thi_truong.py         # Markets
│   ├── chu_vung.py           # Farm owners
│   ├── trang_thai.py         # Status
│   ├── lich_su.py            # Diary entries
│   ├── thong_ke.py           # Statistics
│   ├── chung_nhan.py         # Certificates
│   └── sau_benh.py           # Pest/disease tracking
│
├── routes/                   # API endpoints
│   ├── __init__.py
│   ├── farms.py              # /api/farms endpoints
│   ├── charts.py             # /api/charts endpoints
│   └── diary.py              # /api/diary endpoints
│
├── .env                      # Environment variables
├── requirements-minimal.txt  # Python dependencies
├── test_db.sh               # Database test script
└── start.sh                 # Server start script
```

---

## 🔌 API Endpoints

### 🏥 Health Check
```
GET  /api/health
```

### 🌾 Farms (Vùng Trồng)
```
GET    /api/farms              # List all farms (with pagination)
GET    /api/farms/{id}         # Get farm details
GET    /api/farms/by-code/{ma} # Get farm by MSVT code
POST   /api/farms              # Create new farm
PUT    /api/farms/{id}         # Update farm
DELETE /api/farms/{id}         # Delete farm
```

### 📊 Charts & Statistics
```
GET /api/charts/dashboard-stats    # Dashboard overview
GET /api/charts/export-markets     # Export markets pie chart
GET /api/charts/crop-production    # Crop production bar chart
GET /api/charts/productivity-trend # Productivity line chart
GET /api/charts/farm-status        # Farm status distribution
GET /api/charts/activity-timeline  # Activity timeline
```

### 📝 Diary (Nhật Ký Canh Tác)
```
GET    /api/diary                 # List diary entries
GET    /api/diary/{id}            # Get entry details
POST   /api/diary                 # Create new entry
PUT    /api/diary/{id}            # Update entry
DELETE /api/diary/{id}            # Delete entry
GET    /api/diary/activity-types  # List activity types
```

---

## 📖 Ví Dụ Sử Dụng

### Lấy Danh Sách Vùng Trồng

```bash
curl http://localhost:8000/api/farms?limit=10
```

```javascript
// Frontend (Fetch API)
const response = await fetch('http://localhost:8000/api/farms?limit=10');
const data = await response.json();
console.log(data);
```

### Tạo Vùng Trồng Mới

```bash
curl -X POST http://localhost:8000/api/farms \
  -H "Content-Type: application/json" \
  -d '{
    "ma_vung": "MSVT2024001",
    "ten_vung": "Vườn Xoài Cát Chu",
    "dia_chi": "Xã Tân Thạnh, Huyện Châu Thành",
    "dien_tich_ha": 5.2,
    "toa_do": [
      {"latitude": 10.123, "longitude": 106.456, "thu_tu": 1},
      {"latitude": 10.124, "longitude": 106.457, "thu_tu": 2}
    ]
  }'
```

### Lấy Biểu Đồ Thị Trường

```javascript
// Vue.js example
async function loadMarketChart() {
  const response = await fetch('http://localhost:8000/api/charts/export-markets');
  const chartData = await response.json();
  // Use with Chart.js or any chart library
  return chartData;
}
```

---

## 🔧 Configuration

### Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `DB_HOST` | PostgreSQL host | localhost |
| `DB_PORT` | PostgreSQL port | 5433 |
| `DB_NAME` | Database name | postgres |
| `DB_USER` | Database user | postgres |
| `DB_PASSWORD` | Database password | 123456 |
| `DB_SCHEMA` | Schema name | nongsan |
| `API_PREFIX` | API route prefix | /api |
| `CORS_ORIGINS` | Allowed origins | localhost:5173,5174 |

### Password Options

Thử 2 password phổ biến:
- `123456` (default)
- `000000` (alternative)

---

## 🐛 Troubleshooting

### ❌ Database Connection Failed

```bash
# 1. Kiểm tra PostgreSQL có chạy không
brew services list | grep postgres

# 2. Test connection bằng psql
psql -h localhost -p 5433 -U postgres -d postgres

# 3. Chạy test script
./test_db.sh
```

### ❌ Module Not Found

```bash
# Cài lại dependencies
pip install -r requirements-minimal.txt

# Kiểm tra virtual environment
which python  # Should show .venv/bin/python
```

### ❌ Port Already in Use

```bash
# Tìm process đang dùng port 8000
lsof -i :8000

# Kill process
kill -9 <PID>

# Hoặc dùng port khác
uvicorn app:app --port 8001
```

---

## 🔐 Security Notes

**⚠️ QUAN TRỌNG:**
- Đổi `SECRET_KEY` trong production
- Không commit `.env` vào git
- Sử dụng password phức tạp cho database
- Giới hạn CORS origins trong production

---

## 📦 Dependencies

### Core
- **FastAPI** 0.115.6 - Web framework
- **Uvicorn** 0.34.0 - ASGI server
- **SQLAlchemy** 2.0.39 - ORM
- **Pydantic** 2.10.3 - Data validation

### Database
- **psycopg2-binary** 2.9.10 - PostgreSQL adapter
- **Alembic** 1.17.0 - Database migrations

### Utilities
- **python-dotenv** 1.1.0 - Environment variables
- **pandas** 2.3.2 - Data processing
- **numpy** 2.1.3 - Numerical operations

---

## 🚦 Development Workflow

### 1. Test Database Connection
```bash
./test_db.sh
```

### 2. Start Development Server
```bash
./start.sh
```

### 3. Test Endpoints
Open http://localhost:8000/docs

### 4. Check Logs
Server logs xuất hiện trong terminal

---

## 📞 Support

- **API Docs:** http://localhost:8000/docs
- **Database Schema:** See `/Database/README.md`
- **Frontend Integration:** See `/Frontend/README.md`

---

**✨ Built with FastAPI + PostgreSQL + SQLAlchemy**
